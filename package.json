import React from "react";
import { motion } from "framer-motion";
import { Music, Disc3 } from "lucide-react";
import { PLAYLIST } from "../data/content";

export default function PlaylistSection({ musicStarted }) {
  return (
    <section
      id="playlist"
      className="section-anchor relative py-24 md:py-32 px-6"
      data-testid="playlist-section"
    >
      <div className="max-w-3xl mx-auto">
        <motion.div
          initial={{ opacity: 0, y: 14 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{ duration: 0.6 }}
          className="text-center mb-12"
        >
          <div className="inline-flex items-center gap-2 px-4 py-1.5 rounded-full bg-pink-100/70 text-pink-700 text-xs font-semibold tracking-wider mb-4">
            <Music size={14} />
            TRILHA SONORA
          </div>
          <h2 className="font-heading text-3xl md:text-5xl font-600 text-purple-900">
            {PLAYLIST.title}
          </h2>
          <p className="mt-3 text-purple-800/70 max-w-xl mx-auto">
            {PLAYLIST.description}
          </p>
        </motion.div>

        <motion.div
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{ duration: 0.6 }}
          className="relative glass-card rounded-3xl p-8 md:p-12 text-center overflow-hidden"
          data-testid="playlist-card"
        >
          {/* Background gradient sparkle */}
          <div
            aria-hidden
            className="absolute inset-0 opacity-50 pointer-events-none"
            style={{
              backgroundImage:
                "radial-gradient(circle at 20% 30%, rgba(236,72,153,0.18) 0, transparent 50%), radial-gradient(circle at 80% 70%, rgba(245,158,11,0.18) 0, transparent 50%)",
            }}
          />

          {/* Big spinning record */}
          <div className="relative z-10 flex flex-col items-center">
            <div className="relative">
              <div
                className={`relative h-44 w-44 md:h-56 md:w-56 rounded-full flex items-center justify-center shadow-[0_20px_60px_rgba(147,51,234,0.35)] ${
                  musicStarted ? "spin-slow" : ""
                }`}
                style={{
                  background:
                    "radial-gradient(circle at 30% 25%, #4C1D95 0%, #1E1B4B 70%, #0F0A2E 100%)",
                }}
              >
                {/* Grooves */}
                <div className="absolute inset-3 rounded-full border border-white/5" />
                <div className="absolute inset-6 rounded-full border border-white/5" />
                <div className="absolute inset-10 rounded-full border border-white/5" />
                <div className="absolute inset-14 rounded-full border border-white/5" />

                {/* Center label */}
                <div className="h-16 w-16 md:h-20 md:w-20 rounded-full bg-gradient-to-br from-pink-400 to-purple-500 ring-8 ring-purple-900 flex items-center justify-center">
                  <Disc3 size={28} className="text-white/80" />
                </div>
              </div>
            </div>

            <div className="mt-8">
              <p className="text-[11px] uppercase tracking-[0.3em] font-bold text-purple-500/70 mb-2">
                tocando agora
              </p>
              <h3 className="font-heading text-2xl md:text-3xl font-600 text-purple-900">
                {PLAYLIST.songName}
              </h3>
              <p className="mt-3 text-sm text-purple-700/70 max-w-md mx-auto">
                {musicStarted
                  ? "A música está tocando de fundo enquanto você lê. 💜"
                  : "Toque o envelope na tela inicial pra começar a música."}
              </p>
            </div>
          </div>
        </motion.div>
      </div>
    </section>
  );
}
